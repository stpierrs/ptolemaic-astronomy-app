# Change log

Detailed change history is maintained in the git commit log (`git log --oneline`).

The model runs on Ptolemy's deferent + epicycle ephemeris (Almagest).
AstroPixels eclipse catalogues are reserved for eclipse-demo refinement
only.
